%h3= quiz.name
by
= link_to quiz.user.email, quiz.user

%ol
	- quiz.questions.each do |q|
		%li= q.title

%p.subtle= "#{quiz.results.count} participants"

= link_to "Enter Quiz", enter_quiz_path(quiz), class: "button primary"
= link_to "Edit Quiz", edit_quiz_path(quiz), class: "button"